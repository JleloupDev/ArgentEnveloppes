name: argentenveloppes
description: Application de gestion de budget personnel utilisant la méthode des enveloppes budgétaires.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  riverpod: ^2.3.6
  flutter_riverpod: ^2.3.6
  firebase_core: ^3.13.1 # Mise à jour
  firebase_auth: ^5.5.4 # Mise à jour
  google_sign_in: ^6.2.1 # Mise à jour
  cloud_firestore: ^5.6.8 # Mise à jour

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mockito: ^5.4.0
  build_runner: ^2.3.3

flutter:
  uses-material-design: true

